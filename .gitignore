# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# PyInstaller build output (CatchRadar.spec IS committed)
build/
dist/

# build-generated version stamp + code-patch artifacts
code_version.txt
app-code.zip
app-code-stage/

# Per-user runtime data (local SQLite DB + logs are not source)
data/
output/

# OS / editor noise
.DS_Store

# local render / verification artifacts
_*.png

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Claude Code local-only settings
.claude/settings.local.json

# Editor/tool caches and local artifacts (machine-specific, not source)
.codegraph/
.cursor/
logs/
verification/

# Proxy subscription export — contains live node URLs/credentials, never commit
shadowrocket_nodes_export.json
